
// console.log(window.location)
console.log("host", location.host);
console.log("hostname", location.hostname);
console.log("href", location.href);
console.log("origin", location.origin);
console.log("pathname", location.pathname);
console.log("port", location.port);
console.log("protocol", location.protocol);
console.log("search", location.search);

// location.reload();

// location.assign("https://www.google.com");
// location.replace("https://www.google.com");


// assign() method loads a new document.
// replace() method replaces the current document with a new one.

// assign vs replace => assign() method loads a new document, replace() method replaces the current document with a new one. The difference is that assign() keeps the current document in the session history, meaning the user can go back to it, while replace() does not.
