
const parentEl = document.querySelector(".outerClass");
// 1. firstElementChild

console.log(parentEl.firstElementChild);    // it returns the first element child of the parent element

// 2. lastElementChild

console.log(parentEl.lastElementChild);    // it returns the last element child of the parent element


// 3. firstChild

console.log(parentEl.firstChild);    // it returns the first child node of targeted element i.e node means text,comment,HTML element

// 4. lastChild

console.log(parentEl.lastChild);    // it returns the last child node of targeted element i.e node means text,comment,HTML element
